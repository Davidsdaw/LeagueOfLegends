CREATE TABLE USUARIOS (
    user VARCHAR(50) PRIMARY KEY,
    password VARCHAR(50) NOT NULL,
    rol CHAR(1) CHECK (rol IN ('A', 'R')), -- 'A' para administradores, 'R' para registrados
    mail VARCHAR(100) NOT NULL
);

INSERT INTO USUARIOS (user, password, rol, mail) VALUES 
('admin', '1234', 'A', 'admin@riberadeltajo.es'),
('registrado', '1234', 'R', 'registrado@riberadeltajo.es');

CREATE USER 'admin'@'%' IDENTIFIED VIA mysql_native_password USING '***';GRANT ALL PRIVILEGES ON *.* TO 'admin'@'%' REQUIRE NONE WITH GRANT OPTION MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0;GRANT ALL PRIVILEGES ON `leagueoflegends`.* TO 'admin'@'%';